# 2026-09-07 · 选型与改进复盘

## 为什么选择这条路线

数字人 PoC 的价值不只在于“嘴会动”：台词版本、字幕时长、音频可用性和失败报告也影响验收。原仓库有操作流程与 FFmpeg 脚本，但没有 README 和可自动复现的验收入口。此次为既有作品增加一个独立的质量检查环节。

## 候选开源项目

以下为 2026-09-07 检索时 GitHub 页面显示的约数，不是实时排行榜，也不代表质量评分。

| 项目 | Stars 约数 | 适用任务 | 本次决策 |
|---|---:|---|---|
| [LangGraph](https://github.com/langchain-ai/langgraph) | 41.1k | 有状态 Agent、检查点与人工介入 | 适合下一轮售前 Agent 工作流迁移；本次不声称已使用 |
| [Pipecat](https://github.com/pipecat-ai/pipecat) | 15.3k | 实时语音、多模态流水线 | 适合语音交互和打断实验，需服务及延迟实测 |
| [LiveTalking](https://github.com/lipku/LiveTalking) | 9.4k | 实时数字人交互 | 本次实现接口请求导出；未复制其引擎 |
| [MuseTalk](https://github.com/TMElyralab/MuseTalk) | 6.5k | 口型同步与视频生成 | 更偏模型推理；需 GPU、权重和素材，不把本地素材验收当作推理复现 |

## 已实现的差异

1. 从“人工看文件”变为输入格式与交付条件检查，输出 JSON 和明确退出码。
2. 对字幕采用严格连续序号、合法时间、正时长、无重叠校验；字幕尾帧不能越过音频。
3. 文稿比较仅消除空白，保留数字和标点，避免价格变化漏检。
4. WAV 实际帧数与头部校验，检测截断、静音和明显削波；音频采用流式样本统计。
5. 以 `echo` 输出 LiveTalking 请求参数，避免下游 LLM 再次改写已核稿台词。默认不打断。
6. 用文件 SHA-256 标识具体输入版本。报告只验证其生成时的文件，后续修改必须重跑。

## 验证与限制

本地 Python 3.9.6：14 个测试通过。成功和失败样例实际执行；音频是数学合成测试音。静音/削波规则不能判断是否有人声，尚未检测直流偏置、噪声、说话人或音色一致性。没有 ASR 对齐、VAD、LUFS、口型同步指标，也没有 LiveTalking HTTP 联调、WebRTC、GPU 吞吐或端到端延迟实测。主路径实现零依赖，方便面试官复现；依赖模型的能力保留为下一阶段。

本次适配依据 [LiveTalking 路由源码](https://github.com/lipku/LiveTalking/blob/main/server/routes.py)，其代码许可由上游管理；本项目未复制引擎或权重。实际集成还需检查使用的各模型及素材许可。

## 下一阶段可验收任务

- 锁定 LiveTalking 版本，启动一个真实会话，验证请求成功、无效会话和超时路径。
- 用自己授权的正脸素材和台词生成短视频，保存配置、运行日志和样片。
- 采集首音延迟、首帧延迟和打断停止时间，各至少 30 次，报告设备和 P50/P95；当前不填写虚构结果。
- 接入 ASR 对齐，统计字错率和字幕偏移；把模型检查与格式检查分开报告。

## 面试讲解（按实际掌握程度使用）

“我围绕数字人口播交付，补充了无需 GPU 即可复现的素材验收模块，将音频、字幕、台词版本核对变成可执行规则，并按 LiveTalking 接口导出已核稿台词。14 项测试覆盖正常、损坏输入和交付失败路径。目前验证到素材与请求参数层，真实渲染和延迟评测仍是下一步。”

重点演示：先跑 demo，打开失败报告解释字幕为何超时；将 199 改为 99 重跑；最后展示请求导出与实际播放之间的边界。
